// TimeAgoParser module — port of NewPipeExtractor's timeago-parser
// (timeago-parser/src/main/java/org/schabi/newpipe/extractor/timeago).
//
// Contains PatternsHolder/PatternsManager and the generated per-locale
// pattern classes (English first; the remaining locales are mechanical and
// land in bulk later). The TimeAgoParser *class* itself lives in the
// SwiftPipeExtractor module (localization/TimeAgoParser.swift), mirroring
// upstream's layout.
